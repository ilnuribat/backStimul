enum SEARCH_TYPE {
  MESSAGES
  TASKS
  OBJECTS
  USERS
}

type SearchType {
  messages: [Message]
  objects: [Object]
  tasks: [Task]
  users: [User]
  files: [File]
}

type Query {
  search(query: String!, limit: Int): SearchType
}