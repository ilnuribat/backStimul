  type File {
    id: ID!
    fileId: String
    filename: String
    mimetype: String
    length: Int
  }

  type Query {
    uploads: [File],
    findFiles (id: ID!): [File],
  }

  type Mutation {
    uploadFile(id: ID!, file: Upload!): File!
  }
