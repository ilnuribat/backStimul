type IDName {
  id: ID
  name: String
}

type shortnameDescription {
  shortName: String
  description: String
}

type Glossary {
  taskStatuses: [IDName]
  abstractResource(id: ID!): String
  SU: [IDName]
  constructionTypes: [shortnameDescription]
}

type Query {
  glossary: Glossary
}
