type User {
  id: Int!
  createdAt: String!
  updatedAt: String!
  username: String!
  email: String!
  projects: [Project]!
}

type Query {
  user(id: Int): User!
  users: [User]
}

type Mutation {
  updateUser (id: Int!, username: String, email: String): User!
}
