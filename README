## backend

### requirements
```
  git
  docker
  docker-compose
  nodejs:10
```

### шаги для старта backend-а

```
git pull git@bitbucket.org:guov/backstimul.git
cd backstimul 
npm i
cp .env.example .env
npm run databases:up
npm run migrate:up
npm run dev
```
