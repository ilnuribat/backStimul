import React, { Component } from 'react'
import PropTypes from 'prop-types'

/** NavParts view */

class NavParts extends Component {
  constructor(props) {
    super(props)
  
    this.state = {
    }
  }

  static propTypes = {
  }

  render() {
    if(true){
      return( "NavParts" )
    }else{
      return ( "NavParts" )
    }
  }
}
export default NavParts